#!/usr/bin/make -f
# -*- makefile -*-

override_dh_auto_configure:
	if [ -f bootstrap.sh ]; then $(CURDIR)/bootstrap.sh; fi
	$(CURDIR)/configure CPPFLAGS="-DHAVE_INTTYPES_H -DHAVE_NETINET_IN_H" --prefix=/usr --with-thriftpath=/usr
%:
	dh $@

